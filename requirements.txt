# ============================================================
# AURA Requirements — Core Only
# ============================================================
# This file lists ONLY the packages needed for basic CLI usage:
#   pip install -r requirements.txt
#
# For optional features, install extras via pyproject.toml:
#   pip install .[ml]          — PyTorch, transformers, embeddings
#   pip install .[api]         — FastAPI web server
#   pip install .[voice]       — Text-to-speech, audio
#   pip install .[windows]     — Windows-specific (notifications, brightness, audio control)
#   pip install .[telegram]    — Telegram bot integration
#   pip install .[tools]       — PDF, clipboard, screenshots, image hashing
#   pip install .[calendar]    — iCal / scheduling
#   pip install .[memory]      — Knowledge graph (networkx)
#   pip install .[security]    — Cryptography for token encryption
#   pip install .[scheduler]   — APScheduler for background tasks
#   pip install .[treesitter]  — AST-based code search
#   pip install .[all]         — Everything
#
# Core install is ~200MB. Full install (.[all]) is ~4GB (mostly PyTorch).
# All optional imports use try/except so missing deps degrade gracefully.
# ============================================================

# --- Core (required for aura CLI) ---
ollama>=0.4.0,<1.0.0
requests>=2.31.0,<3.0.0
pydantic>=2.0.0,<3.0.0
python-dotenv>=1.0.0,<2.0.0
psutil>=5.9.0,<7.0.0
pyyaml>=6.0,<7.0
tenacity>=8.2.0,<10.0.0
rich>=13.0.0,<14.0.0
prompt_toolkit>=3.0.0,<4.0.0
