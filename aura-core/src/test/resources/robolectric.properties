# Library modules have no targetSdk, so Robolectric derives the emulated SDK
# from compileSdk (37) — above Robolectric 4.16's maximum supported SDK (36).
# Pin the default emulated SDK; per-test @Config(sdk = [...]) still overrides.
sdk=36
